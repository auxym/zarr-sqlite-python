[project]
name = "zarr-sqlite"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Francis Thérien", email = "frtherien@gmail.com" }
]
requires-python = ">=3.13"
dependencies = [
    "zarr>=3.1.0",
]

[build-system]
requires = ["uv_build>=0.8.3,<0.9.0"]
build-backend = "uv_build"
